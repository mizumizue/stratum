---
description: L1 憲法ディレクトリの編集制約
globs: specs/L1/**
alwaysApply: false
---

# L1 編集制約（Cursor adapter）

**正本:** `agents/policy/l1-immutable.md`。

このパスは Agent が直接編集しない。ユーザーが Steward を明示し承認した場合のみ変更する。変更時は `VERSION` と各ファイルの version を揃える。
